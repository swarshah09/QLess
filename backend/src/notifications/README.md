# notifications

Notification delivery (Web Push via VAPID, in-app, email/SMS adapters) and rule
evaluation live here. Part 1 only defines the data models
(`NotificationRule`, `PushSubscription`, `NotificationEvent`) — no delivery code
is implemented yet.
